(jbuild_version 1)

; (executable
;  (
;  (name hpdr)
;  (libraries (core sexplib))
;  (preprocess (pps (ppx_inline_test ppx_jane)))
;  )
; )

(library
 (
 (name hpdr)
 (libraries (core xml-light z3))
 (inline_tests)
  ; [XXX] use camlon
 (preprocess (pps (ppx_inline_test ppx_deriving.show)))
 )
)
