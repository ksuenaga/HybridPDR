(jbuild_version 1)

; (executable
;  (
;  (name hpdr)
;  (libraries (core sexplib))
;  (preprocess (pps (ppx_inline_test ppx_jane)))
;  )
; )

(library
 (
 (name hpdr)
 (libraries (core xml-light Z3 mparser sexplib mparser.re odepack bau))
 (inline_tests)
  ; [XXX] use camlon
 (preprocess (pps (ppx_inline_test ppx_deriving.show bisect_ppx)))
 )
)
